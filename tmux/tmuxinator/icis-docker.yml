# ~/.tmuxinator/icis-docker.yml
name: icis-docker
root: ~/src/docker-dev/
windows:
  - docker:
      layout: main-horizontal
      panes:
        - cd ~/src/docker-dev;
  - snowflake:
      layout: main-horizontal
      panes:
        - cd ~/src/snowflake;
  - staff:
      layout: main-horizontal
      panes:
        - cd ~/src/icisstaff;
  - bouncah:
      layout: main-horizontal
      panes:
        - cd ~/src/bouncah;
